[package]
name = "kontor-core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "Domain identifiers, states and commands for the Kontor control plane."

[lints]
workspace = true

[features]
# Test harnesses may opt into an explicit deterministic UUIDv7 source. The
# production default remains the wall-clock-backed `Uuid::now_v7` path.
deterministic-test-ids = []

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
uuid = { workspace = true }
jiff = { workspace = true }
thiserror = { workspace = true }
sha2 = { workspace = true }
hex = { workspace = true }

[dev-dependencies]
proptest = { workspace = true }
