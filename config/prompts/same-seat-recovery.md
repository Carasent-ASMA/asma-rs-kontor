# Same-seat recovery

Reconcile the existing seat and native session. Preserve its evidence and reuse
it when usable. Replace it only after it is proved retired or unusable; never
create a second non-terminal session for the same role slot.
