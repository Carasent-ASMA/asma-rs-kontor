VERDICT: PASS

# QA Report — KON-OP-08 / ASMA-7877 (qa-gate re-run)

- Date: 2026-08-22
- Tester seat: qa-gate (deepseek-v4-flash; role qa-gate only — no code review, no implementation, no commits)
- Pinned shared submodule: `_tools/asma-rs-kontor` at `/Users/igor/carasent/asma-modules/.worktrees/kon-op-08/_tools/asma-rs-kontor`
- Pinned SHA (HEAD): `d310cdc2be77110b7b88b9fec38d8804aceba05d` (`fix(kontor): Restore replay convergence ASMA-7877`)
- Prior gate SHA `527cc16` (previous Claude QA-REPORT.md) is not reachable in this repo's history; the prior report file `QA-REPORT.md` is not present in this tree (untracked on the previous branch state / superseded). This re-run covers current HEAD + the uncommitted ASMA-7877 patch.
- Patch applied: **YES** — `git diff` of the shared worktree captured to `~/.cache/op08-qa-deepseek/wip.patch` (337 lines), applied cleanly with `git apply` into the private detached worktree. Resulting diff vs pinned HEAD: 4 files changed, **240 insertions(+), 4 deletions(-)** — matches the ~240-line uncommitted ASMA-7877 work described in the run brief.
- Private QA tree: `~/.cache/op08-qa-deepseek/tree` (detached HEAD, patch applied; shared worktree untouched — verified identical status before/after)
- Toolchain: rustc/cargo **1.97.1** (matches `rust-toolchain.toml` pin `1.97.1`, components rustfmt+clippy)
- CARGO_TARGET_DIR: `$HOME/.cache/op08-qa-deepseek/target` (private; shared worktree has no target writes)

## Commands and exit codes

All commands ran from `~/.cache/op08-qa-deepseek/tree` with the CARGO_TARGET_DIR above.

| Command | Exit | Result |
|---|---|---|
| `cargo fmt --all --check` | 0 | clean, no diffs |
| `cargo clippy --all-targets --workspace` | 0 | 0 warnings, 0 errors (full log `~/.cache/op08-qa-deepseek/clippy.log`) |
| `cargo test --workspace` | 0 | 118/118 `test result: ok` blocks; **1640 passed, 0 failed, 8 ignored** (log `~/.cache/op08-qa-deepseek/test-workspace.log`) |
| `cargo test -p kontor-api` | 0 | 21 + 5 + 3 passed, 0 failed (includes `openapi_contract.rs` — 3 passed; OpenAPI contract pin green) |

## DeliveryConfirmationUnknown tests (uncommitted patch)

All three new/patched tests ran in the suite and passed:

- `error::tests::an_unknown_message_confirmation_never_claims_nothing_changed` (kontor-api unit) — **ok**
- `message_an_incomplete_confirmation_scan_never_authorizes_a_resend` (kontor-runtime-paseo `tests/contract.rs`) — **ok**
- `message_a_history_read_promotes_confirmation_unknown_to_a_replayable_ack` (kontor-runtime-paseo `tests/contract.rs`) — **ok**

## Ignored tests (8) — NOT run, by design (live-harness, require external daemons/accounts)

1. `kontor-runtime-ao tests/live.rs` — `live_smoke_launches_two_clients_concurrently` (requires a disposable AO daemon)
2. `kontor-runtime-codex tests/live.rs` — `two_pinned_accounts_run_concurrently_and_neither_ending_closes_a_run` (requires two authenticated Codex accounts + disposable worktree)
3. `kontor-runtime-paseo tests/live.rs` — 6 tests (all require a live Paseo daemon):
   - `live_a_correlated_session_read_round_trips`
   - `live_adopted_root_places_and_archives_a_child_and_registers_no_project`
   - `live_an_unknown_agent_is_refused_rather_than_answered_empty`
   - `live_cli_reports_the_supported_baseline`
   - `live_hello_is_accepted_and_the_daemon_pushes_a_pinned_identity`
   - `live_the_status_readback_agrees_with_the_pushed_identity`

## Coverage note

The previous QA report (QA-REPORT.md, PASS on SHA 527cc16) listed 7 inspector findings that remained uncovered by tests. Per the run brief I did **not** re-do code review and did not re-audit those findings. Statement on the suite: **the suite is still green** — 1640/1640 runnable tests pass on this delivery (HEAD d310cdc + uncommitted patch). Same rule as the previous report applies: a green suite with uncovered inspector findings is still PASS.

## What I could not run / could not verify

- The 8 ignored live-harness tests listed above (require live AO daemon, live Paseo daemon, and two authenticated Codex accounts; explicitly excluded by the brief).
- Playwright / console app: not run — no browser install performed. Not applicable in the end: the diff touches no `openapi.json` or `schema.d.ts` (only `crates/kontor-api/src/error.rs`, `crates/kontor-runtime-paseo/src/adapter.rs`, `crates/kontor-runtime-paseo/tests/contract.rs`, `crates/kontor-runtime/src/adapter.rs`), so no openapi/schema drift check was triggered.
- Re-reading the previous QA-REPORT.md (7 findings wording, exact counts): file absent from this tree — referenced from the run brief only.
- Git range vs prior pinned SHA 527cc16: unknown revision in this repo (prior branch state not present here).

## Verdict rationale

- `cargo fmt --all --check` exit 0 — pass
- `cargo clippy --all-targets --workspace` exit 0, zero warnings — pass
- `cargo test --workspace` exit 0, 0 failures — pass
- `cargo test -p kontor-api` exit 0 incl. OpenAPI contract pin — pass
- WIP patch applies cleanly to pinned HEAD — pass

All gates green → **VERDICT: PASS**.

## Environment / hygiene

- Shared worktree `feat/ASMA-7877-kontor-operational-control-surfaces`: untouched by this run (read-only git operations only; status verified identical before and after). No writes, stages, stashes, resets, or branch switches.
- Logs retained for LSA: `~/.cache/op08-qa-deepseek/` (`wip.patch`, `test-workspace.log`, `test-kontor-api.log`, `clippy.log`, `tree/`). Tree not deleted pending LSA confirmation; no `cargo clean` run anywhere.
