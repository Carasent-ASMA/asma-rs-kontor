# KON-OP-07 QA validation — remediation 1

Date: 2026-08-18 09:56 CEST
Seat: QA · OP-07
Task: `01a0074f-672c-7f70-8bdd-da707dcda0ce` / ASMA-7876
Reviewed tree: `d3ecbecd53fb1e1bf9076946e2f703f7973c9b83`

## Result

**QA evidence passes; QA gate settlement is blocked on Kontor control-plane recovery.**

This validation covers the three remediation claims that remain in OP-07 after
the epic owner re-scoped the native Jira connector work to its own ticket. The
inspector's code-review gate already passed at receipt
`01a013d4-6056-7542-9378-bb5c34332d9a`; this seat independently re-ran the
focused public/store regressions on the same commit.

| Acceptance behaviour | Independent command | Result |
| --- | --- | --- |
| Manifest failure rolls back the entire import and a retry resumes | `cargo test -p kontor-store an_import_that_fails_while_recording_its_manifest_leaves_nothing_and_resumes` | pass (1/1) |
| A legacy-owned backlog refuses graph writes | `cargo test -p kontor-daemon --test loopback_api a_backlog_a_legacy_system_owns_refuses_the_writes_that_are_the_backlog` | pass (1/1) |
| Fresh native projects bootstrap through public operations and the freeze contract reports its actual status | `cargo test -p kontor-daemon --test loopback_api an_empty_realm_is_bootstrapped_through_public_operations_alone` | pass (1/1) |

The tree was unchanged before testing except for pre-existing untracked evidence
owned by other seats (`CODE-REVIEW-01.md` and KON-MVP-18 run directories).

## Remaining observation

`transition_task` calls `require_backlog_authority` before reading or changing
the task, but no regression test currently asserts the withheld-authority
lifecycle path. The inspector mutation-tested the graph-write guard and recorded
this as non-blocking. QA agrees it does not contradict the delivered code, but
the missing fixture should be added before `legacy_pending` backlog origin is
made selectable in checkpoint 5.

## Gate settlement blocker

The required Kontor CLI readback and `gate-record` operations cannot reach the
existing daemon realm. The durable operational-gap record is
`_docs/ai-orchestration/reports/2026-08-18-09-53-report-kontor-op07-qa-control-plane-gap.md`.
Do not infer a passed QA gate from this file. After the daemon recovers, read
back the same task/QA-run identity, attach the gap record, and settle the QA
verdict through Kontor.
