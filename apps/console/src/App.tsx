/**
 * Static placeholder for the Kontor operator console (KON-MVP-02 scaffold).
 *
 * KON-MVP-17 builds the real console against the Kontor API; this component
 * only proves the React/Vite/TypeScript toolchain compiles and renders.
 */
export function App() {
  return (
    <main>
      <h1>Kontor operator console</h1>
      <p>Scaffold placeholder — the interactive console lands in KON-MVP-17.</p>
    </main>
  )
}
